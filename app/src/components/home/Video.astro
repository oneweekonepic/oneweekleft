<section class="video-section" aria-labelledby="video-title">
  <div class="video-inner">
    <div class="video-heading">
      <p class="eyebrow">VIDEO</p>
      <h2 id="video-title">THE WAY YOU MOVE</h2>
    </div>

    <div class="video-frame">
      <video
        src="/videos/the-way-you-move.mp4"
        poster="/images/the-way-you-move-poster.jpg"
        controls
        playsinline
        preload="metadata"
        aria-label="THE WAY YOU MOVE by ONE WEEK LEFT"
      ></video>
    </div>
  </div>
</section>

<style>
  .video-section {
    background: #050505;
    color: #fff;
    padding: 70px 44px 150px;
  }

  .video-inner {
    width: min(1120px, 100%);
    margin: 0 auto;
    display: grid;
    grid-template-columns: minmax(260px, .65fr) 1.35fr;
    gap: clamp(50px, 8vw, 130px);
    align-items: start;
  }

  .video-heading {
    padding-top: 7px;
  }

  .eyebrow {
    margin: 0 0 24px;
    color: #d71920;
    font-size: 10px;
    letter-spacing: .3em;
  }

  h2 {
    margin: 0;
    max-width: 320px;
    font-size: clamp(26px, 2.4vw, 38px);
    line-height: 1.15;
    font-weight: 300;
    letter-spacing: .1em;
  }

  .video-frame {
    width: min(380px, 100%);
    margin: 0 auto;
  }

  video {
    display: block;
    width: 100%;
    height: auto;
    aspect-ratio: 9 / 16;
    background: #000;
  }

  @media (max-width: 800px) {
    .video-section {
      padding: 65px 20px 90px;
    }

    .video-inner {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    h2 {
      font-size: clamp(28px, 9vw, 38px);
    }

    .video-frame {
      width: min(82vw, 380px);
    }
  }
</style>
